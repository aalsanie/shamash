# Shamash PSI examples (schema v1)

These example configs are **schema-valid** and use only **implemented rule ids**:

- naming.bannedSuffixes
- arch.forbiddenRoleDependencies
- metrics.maxMethodsByRole
- packages.rolePlacement
- packages.rootPackage
- deadcode.unusedPrivateMembers

Each file focuses on a specific capability (matchers, scope filters, rule params, exceptions, etc.).
